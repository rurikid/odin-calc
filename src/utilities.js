class Utilities {
  static clamp = (val, min, max) => Math.min(Math.max(val, min), max);

  // break input into tokenized actionable elements
  // breakers
  // +, -, *, /, SIN, COS, TAN, INV, SQRT, ()
  static (input) {

  }
}

export { Utilities };